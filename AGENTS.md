# Agent Communication Protocol / 协作与回复协议

This repository is maintained together with AI coding agents (Codex, Claude
Code, and similar). Every reply, task summary, and PR description addressed to
the maintainer (Victor, @zhangzhongnan928) must follow the protocol below.

## 0. 总原则

维护者充分授权，但注意力极少：他只会扫一眼你的回复。
目标：**每条回复 2 秒内可判断结果；每个问题都自带答案；绝不交半成品。**

## 1. 状态行：回复第一行定生死

每次回复（包括 PR 描述的第一行）必须以状态行开头，四选一：

- `✅ 完成 — <一句话：做了什么 + 关键证据>`
- `⚠️ 完成但注意 — <一句话：结果 + 哪里需要留意>`
- `❌ 受阻 — <一句话：卡在哪 + 需要你做什么>`
- `❓ 需要拍板 — <一句话问题>（建议：<推荐答案>）`

状态行之后按固定顺序展开（空的部分整节省略）：

1. **需要你做的** — 最多 3 条，每条一行；不需要维护者动手就不写这一节。
2. **要点** — 最多 5 行，说人话，不堆术语、不贴代码。
3. **细节** — 永远放最后，PR/issue 里用 `<details>` 折叠；默认没人读，仅供追溯。

## 2. 提问：问题必须自带答案

- 能用合理默认推进的，不要问；把所做的假设写进「要点」。
- 必须问时，每个问题固定三件套：**推荐选项 + 一句话理由 + 不回复时的默认动作**。
  例：`❓ 结算币种用 USDC 还是 USDT？建议 USDC（现有合约已集成）。不回复则按 USDC 继续。`
- 给多个选项时，推荐项排第一并标「推荐」，维护者只需点第一个。
- 一次最多 3 个问题，合并成一条消息，不分批打扰。

## 3. 接单：先复述，后闭环

- 开工时用不超过 2 行复述「目标 + 完成标准」，然后直接开工，不等确认。
- 一次交付完整闭环：实现 → 自测/构建 → 提交 → PR。不交半成品。
- 途中发现范围外的问题：先完成本任务，在「要点」末尾用一行提出，不展开、不顺手改。

## 4. 证据优先，叙述靠后

- 用可核验的信号代替解释：测试通过数、CI 状态、截图、diff 规模、PR 链接。
- 「应该可以工作」不算完成，跑过才算；没条件跑就用 ⚠️ 状态行明说漏了什么没验证。

## 5. 语言

- 给维护者的回复、状态行、要点：中文。
- 代码、注释、提交信息、PR 标题、分支名：英文，遵循仓库现有惯例。
